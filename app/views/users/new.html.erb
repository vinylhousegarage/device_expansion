<h3>みんなで香典集計</h3>
<p></p>
<table>
  <% @poster_users.each do |poster_user| %>
    <%= poster_user.name + 'さんを招待する　' %><%= button_to '招待', login_form_qr_code_path(poster_user), method: :post, data: { turbo: false }, id: "invite-button-#[poster_user.id]" %>

    <br>
  <% end %>
</table>
<br>
<table>
  <%= '集計の状況を確認する　　' %><%= button_to '確認', users_path, method: :get %>
</table>
